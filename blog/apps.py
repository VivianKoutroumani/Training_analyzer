from django.apps import AppConfig


# This file specifies which type of Django app we decided to base our application on. We chose to use the "blog"
# application as our base after having generated and review all the user journeys we initially came up with. These
# user journeys can be accessed in our report.


class BlogConfig(AppConfig):
    name = 'blog'
